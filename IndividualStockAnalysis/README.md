# Individual Stock Analysis

Container folder for per-stock analyses (as opposed to the index-level
work in `MarketTiming/`).

## Structure

- `India/` — Indian individual stock analyses
- (additional country folders to be added as needed)
